@model COMP2084Project.Models.ShowEntry

@{
    ViewData["Title"] = "Edit this Entry";
}

<h1>Edit this Entry</h1>

<h4>Edit</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ShowEntryId" />
            <div class="form-group">
                <label asp-for="position" class="control-label"></label>
                <input asp-for="position" class="form-control" />
                <span asp-validation-for="position" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShowId" class="control-label">Show Title</label>
                <select asp-for="ShowId" class="form-control" asp-items="ViewBag.ShowId">Shows</select>
                <span asp-validation-for="ShowId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShowListId" class="control-label">List Name</label>
                <select asp-for="ShowListId" class="form-control" asp-items="ViewBag.ShowListId"></select>
                <span asp-validation-for="ShowListId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
